export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// (getLoginUrl removido — era código morto de um portal OAuth externo não usado;
//  a autenticação do app é via Supabase, em contexts/AuthContext.tsx.)
